# frozen_string_literal: true

source 'https://rubygems.org'

# 2.0 系列目前是预发布版，必须写死版本号，否则 bundler 只会解析到 1.x。
# 下面的 demo 用的是 2.0 的 API（with_tools / parameter），跟 1.x 不兼容。
gem 'ruby_llm', '2.0.0.rc3'

gem 'dotenv', '~> 3.1'
